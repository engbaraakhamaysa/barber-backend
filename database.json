{
  "dev": {
    "driver": "pg",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}"
  },

  "test": {
    "driver": "pg",
    "host": "${TEST_DB_HOST}",
    "port": "${TEST_DB_PORT}",
    "user": "${TEST_DB_USER}",
    "password": "${TEST_DB_PASSWORD}",
    "database": "${TEST_DB_NAME}"
  }
}
