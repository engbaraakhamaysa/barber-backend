{
  "dev": {
    "driver": "pg",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}"
  },

  "test": {
    "driver": "pg",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME_TEST}"
  }
}
